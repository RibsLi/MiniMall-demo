<!--components/recommends/recommends.wxml-->
<view class="recommend">
  <block wx:for="{{recommends}}" wx:key="index">
    <view class="recommend-item">
      <image src="{{item.image}}" class="image"></image>
      <text class="text">{{item.title}}</text>
    </view>
  </block>
</view>
